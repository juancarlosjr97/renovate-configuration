{
    "$schema": "https://docs.renovatebot.com/renovate-schema.json",
    "extends": [
        // In order to avoid circular dependency, the extended configuration does not use the hash but the default branch
        "github>juancarlosjr97/renovate-configuration"
    ]
}
